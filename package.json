{
  "name": "weread-agent-cli",
  "version": "0.1.2",
  "description": "Agent-friendly CLI based on officially supported WeRead APIs.",
  "type": "module",
  "author": "shiquda",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/shiquda/weread-cli.git"
  },
  "bugs": {
    "url": "https://github.com/shiquda/weread-cli/issues"
  },
  "homepage": "https://github.com/shiquda/weread-cli#readme",
  "bin": {
    "weread": "dist/cli.js"
  },
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist",
    "skills"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "dependencies": {
    "commander": "^14.0.2"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
